# Yorutsuke v2 Infrastructure - Deployment Configuration
# Copy to .env or set these environment variables before deployment

# ============================================================
# Azure Document Intelligence (Optional)
# ============================================================
# Enable Azure Document Intelligence in Lambda for model comparison testing
#
# Requirements:
# 1. Endpoint: Your Azure Document Intelligence resource endpoint
# 2. API Key: Your Azure API key
#
# ⚠️  SECURITY NOTE:
# - These are loaded from environment at deployment time
# - NOT hardcoded in CDK stack (safe for git)
# - Do NOT commit .env file (add to .gitignore)
# - Use AWS Secrets Manager for production
#
# Setup:
# 1. Set these in your shell before deploying:
#    export AZURE_DI_ENDPOINT=https://rj0088.cognitiveservices.azure.com/
#    export AZURE_DI_API_KEY=your-api-key-here
#
# 2. Or create .env file in infra/ directory:
#    AZURE_DI_ENDPOINT=https://rj0088.cognitiveservices.azure.com/
#    AZURE_DI_API_KEY=your-api-key-here
#
# 3. Then deploy:
#    npm run deploy --context env=dev --profile dev
#
# If these are not set, Azure DI will not be included in Lambda
#

AZURE_DI_ENDPOINT=https://rj0088.cognitiveservices.azure.com/
AZURE_DI_API_KEY=your-api-key-here
