# Yorutsuke v2 Environment Variables
# Copy to .env.local and fill in values from CDK outputs
# Or run: npm run env:sync (requires AWS CLI)

# ============================================================
# Mock Mode
# ============================================================
# Mock mode is controlled via Debug panel (developer-only):
# 1. Start app: npm run tauri dev
# 2. Type 'debug' anywhere to unlock Debug panel
# 3. Navigate to Debug tab → Mock Mode dropdown
#
# Modes:
# - Off (Real API): Use real backend
# - Online (Mock API): Return fake data, no network calls
# - Offline (Network Fail): Simulate network failure
#
# Setting persists to SQLite and restores on app restart.
# See docs/operations/DEBUG_PANEL.md for details.
# ============================================================

# AWS Region
VITE_AWS_REGION=ap-northeast-1

# Cognito
VITE_USER_POOL_ID=
VITE_USER_POOL_CLIENT_ID=

# Lambda URLs (API Gateway endpoints)
VITE_LAMBDA_PRESIGN_URL=
VITE_LAMBDA_CONFIG_URL=
VITE_LAMBDA_TRANSACTIONS_URL=
VITE_LAMBDA_QUOTA_URL=

# Auth URLs
VITE_AUTH_LOGIN_URL=
VITE_AUTH_REGISTER_URL=
VITE_AUTH_VERIFY_URL=
VITE_AUTH_REFRESH_URL=
