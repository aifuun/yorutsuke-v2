# Mock mode configuration for UI development
# Usage: cp .env.mock .env.local

# Enable mock mode explicitly
VITE_USE_MOCK=true

# No backend URLs needed in mock mode
# All API calls will return realistic fake data
